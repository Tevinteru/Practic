on: 
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: sync
      uses: pressidium/lftp-mirror-action@v1
      with:
        host: alecksaq.beget.tech
        port: 22
        user: alecksaq_s
        pass: ${{ secrets.ftp_password }}
        localDir: '.'
        remoteDir: './project'
        reverse: true
       
