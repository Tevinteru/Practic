on: 
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    - name: Deploy file
      uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      with:
            username: alecksaq_s
            server: alecksaq.beget.tech
            port: 22
            local_path: '.'
            remote_path: './project'
            sftp_only: true
            password: ${{ secrets.FTP_PASSWORD }}
       
